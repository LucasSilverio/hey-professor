
# Rodar o phpstan

./vendor/bin/phpstan

if [ $? -ne 0 ]; then
    echo "Opa! Deu ruim no phpstan. Arrume antes de continuar";
    exit 1;
fi;

# Pega todos os arquivos alterados no commit
# STAGED_FILES=$(git diff --cached --name=only --diff-filter=ACM | grep ".php\{0,1\}")